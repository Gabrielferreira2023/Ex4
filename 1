#include <stdio.h>

int maximo(int num1, int num2);

int main() {
    int a = 5;
    int b = 10;
    int maior = maximo(a, b);
    printf("O maior número é: %d\n", maior);
    return 0;
}

int maximo(int num1, int num2) {
    if (num1 > num2) {
        return num1;
    } else {
        return num2;
    }
}
